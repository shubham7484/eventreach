{
  "name": "eventreach",
  "version": "1.0.0",
  "scripts": {
    "build": "npm run build --prefix shared && npm run build --prefix backend",
    "build:all": "npm run build --prefix shared && npm run build --prefix backend && npm run build --prefix frontend",
    "build:shared": "npm run build --prefix shared",
    "build:backend": "npm run build --prefix shared && npm run build --prefix backend",
    "build:frontend": "npm run build --prefix shared && npm run build --prefix frontend",
    "start:backend": "npm --prefix backend start"
  },
  "workspaces": [
    "frontend",
    "backend",
    "shared"
  ]
}
